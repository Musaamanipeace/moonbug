
{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Stores user-specific data required for lunar calculations and app personalization.",
      "properties": {
        "birthDate": {
          "type": "string",
          "description": "The user's birth date in YYYY-MM-DD format, used to calculate lunar age.",
          "format": "date"
        }
      },
      "required": [
        "birthDate"
      ]
    },
    "StickyNote": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "StickyNote",
      "type": "object",
      "description": "Stores a user's single, persistent sticky note.",
      "properties": {
        "content": {
          "type": "string",
          "description": "The main text content of the sticky note."
        },
        "updatedAt": {
            "type": "string",
            "description": "Timestamp of the last update.",
            "format": "date-time"
        }
      },
      "required": ["content", "updatedAt"]
    },
    "Post": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Post",
      "type": "object",
      "description": "A link or article shared by a user.",
      "properties": {
        "title": {
          "type": "string",
          "description": "The title of the post."
        },
        "url": {
          "type": "string",
          "format": "uri",
          "description": "The URL of the shared link."
        },
        "summary": {
          "type": "string",
          "description": "A user-provided summary of the content."
        },
        "topic": {
          "type": "string",
          "description": "The category or topic of the post."
        },
        "authorId": {
          "type": "string",
          "description": "The UID of the user who created the post."
        },
        "authorName": {
          "type": "string",
          "description": "The display name of the user who created the post."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of when the post was created."
        },
        "commentCount": {
          "type": "number",
          "description": "The number of comments on the post."
        }
      },
      "required": [
        "title",
        "url",
        "topic",
        "authorId",
        "authorName",
        "createdAt",
        "commentCount"
      ]
    },
    "Snap": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Snap",
      "type": "object",
      "description": "A photograph shared by a user in the Snaps gallery.",
      "properties": {
        "imageUrl": {
          "type": "string",
          "format": "uri",
          "description": "The URL of the uploaded image."
        },
        "description": {
          "type": "string",
          "description": "A brief description of the snap."
        },
        "category": {
          "type": "string",
          "description": "The category of the snap."
        },
        "imageHint": {
          "type": "string",
          "description": "One or two keywords for AI image search."
        },
        "authorId": {
          "type": "string",
          "description": "The UID of the user who uploaded the snap."
        },
        "authorName": {
          "type": "string",
          "description": "The display name of the user who uploaded the snap."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of when the snap was created."
        },
        "likes": {
          "type": "number",
          "description": "The number of likes the snap has received."
        }
      },
      "required": [
        "imageUrl",
        "description",
        "category",
        "authorId",
        "authorName",
        "createdAt",
        "likes",
        "imageHint"
      ]
    },
    "Challenge": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Challenge",
      "type": "object",
      "description": "An official or community-created challenge.",
      "properties": {
        "title": { "type": "string", "description": "The title of the challenge." },
        "description": { "type": "string", "description": "A detailed description of the challenge." },
        "type": { "type": "string", "enum": ["official", "community"], "description": "The type of challenge." },
        "badge": { "type": "string", "description": "A descriptive badge for the challenge (e.g., 'Long-Term')." },
        "progress": { "type": "number", "description": "The completion progress, typically for official challenges (0-100)." },
        "participants": { "type": "number", "description": "The number of users participating in the challenge." },
        "creatorId": { "type": "string", "description": "The UID of the user who created the challenge (for community challenges)." },
        "creatorName": { "type": "string", "description": "The display name of the user who created the challenge." },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp of when the challenge was created." }
      },
      "required": ["title", "description", "type", "participants", "createdAt"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/UserProfile"
      },
      "description": "Stores individual user profiles. The document ID (`{userId}`) matches the Firebase Authentication UID."
    },
    "/users/{userId}/sticky_notes/{noteId}": {
      "schema": {
        "$ref": "#/entities/StickyNote"
      },
      "description": "A subcollection for a user's notes. For now, we only use a single document with the ID 'main'."
    },
    "/posts/{postId}": {
      "schema": {
        "$ref": "#/entities/Post"
      },
      "description": "A collection of all posts shared by the community. The document ID is auto-generated."
    },
    "/snaps/{snapId}": {
      "schema": {
        "$ref": "#/entities/Snap"
      },
      "description": "A collection of all snaps shared by the community. The document ID is auto-generated."
    },
    "/challenges/{challengeId}": {
      "schema": {
        "$ref": "#/entities/Challenge"
      },
      "description": "A collection of all challenges, both official and community-created."
    }
  }
}
