
{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserProfile",
      "type": "object",
      "description": "Stores user-specific data required for lunar calculations and app personalization.",
      "properties": {
        "birthDate": {
          "type": "string",
          "description": "The user's birth date in YYYY-MM-DD format, used to calculate lunar age.",
          "format": "date"
        }
      },
      "required": [
        "birthDate"
      ]
    },
    "Reminder": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Reminder",
      "type": "object",
      "description": "Stores a user's single, persistent reminder note.",
      "properties": {
        "content": {
          "type": "string",
          "description": "The main text content of the reminder."
        },
        "updatedAt": {
          "type": "string",
          "description": "Timestamp of the last update.",
          "format": "date-time"
        },
        "notificationTime": {
          "type": "string",
          "format": "date-time",
          "description": "The scheduled ISO 8601 time for the notification."
        },
        "notificationVoice": {
          "type": "string",
          "description": "The identifier for the notification voice.",
          "enum": [
            "Algenib",
            "Achernar",
            "Enif"
          ]
        }
      },
      "required": [
        "content",
        "updatedAt"
      ]
    },
    "Post": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Post",
      "type": "object",
      "description": "A link or article shared by a user.",
      "properties": {
        "title": {
          "type": "string",
          "description": "The title of the post."
        },
        "url": {
          "type": "string",
          "format": "uri",
          "description": "The URL of the shared link."
        },
        "summary": {
          "type": "string",
          "description": "A user-provided summary of the content."
        },
        "topic": {
          "type": "string",
          "description": "The category or topic of the post."
        },
        "authorId": {
          "type": "string",
          "description": "The UID of the user who created the post."
        },
        "authorName": {
          "type": "string",
          "description": "The display name of the user who created the post."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of when the post was created."
        },
        "commentCount": {
          "type": "number",
          "description": "The number of comments on the post."
        }
      },
      "required": [
        "title",
        "url",
        "topic",
        "authorId",
        "authorName",
        "createdAt",
        "commentCount"
      ]
    },
    "Comment": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Comment",
      "type": "object",
      "description": "A comment on a post.",
      "properties": {
        "postId": {
          "type": "string",
          "description": "The ID of the post this comment belongs to."
        },
        "authorId": {
          "type": "string",
          "description": "The UID of the user who wrote the comment."
        },
        "authorName": {
          "type": "string",
          "description": "The display name of the user who wrote the comment."
        },
        "text": {
          "type": "string",
          "description": "The content of the comment."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of when the comment was created."
        }
      },
      "required": [
        "postId",
        "authorId",
        "authorName",
        "text",
        "createdAt"
      ]
    },
    "Snap": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Snap",
      "type": "object",
      "description": "A photograph shared by a user in the Snaps gallery.",
      "properties": {
        "imageUrl": {
          "type": "string",
          "format": "uri",
          "description": "The URL of the uploaded image."
        },
        "description": {
          "type": "string",
          "description": "A brief description of the snap."
        },
        "category": {
          "type": "string",
          "description": "The category of the snap."
        },
        "imageHint": {
          "type": "string",
          "description": "One or two keywords for AI image search."
        },
        "authorId": {
          "type": "string",
          "description": "The UID of the user who uploaded the snap."
        },
        "authorName": {
          "type": "string",
          "description": "The display name of the user who uploaded the snap."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of when the snap was created."
        },
        "likes": {
          "type": "number",
          "description": "The number of likes the snap has received."
        },
        "likedBy": {
            "type": "array",
            "description": "An array of user UIDs who have liked the snap.",
            "items": { "type": "string" }
        }
      },
      "required": [
        "imageUrl",
        "description",
        "category",
        "authorId",
        "authorName",
        "createdAt",
        "likes",
        "imageHint"
      ]
    },
    "UserEvent": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserEvent",
      "type": "object",
      "description": "A custom event created by a user.",
      "properties": {
        "title": { "type": "string", "description": "The title of the event." },
        "location": { "type": "string", "description": "The location of the event, can be a physical address or 'Online'." },
        "date": { "type": "string", "format": "date", "description": "The optional date of the event in YYYY-MM-DD format." },
        "authorId": { "type": "string", "description": "The UID of the user who created the event." },
        "createdAt": { "type": "string", "format": "date-time", "description": "Timestamp of when the event was created." },
        "notificationTime": { "type": "string", "format": "date-time", "description": "Optional scheduled ISO 8601 time for a notification." },
        "notificationVoice": { "type": "string", "enum": ["Algenib", "Achernar", "Enif"], "description": "The optional identifier for the notification voice." },
        "highlighted": { "type": "boolean", "description": "Indicates if the user has highlighted this event." }
      },
      "required": ["title", "authorId", "createdAt"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/UserProfile"
      },
      "description": "Stores individual user profiles. The document ID (`{userId}`) matches the Firebase Authentication UID."
    },
    "/users/{userId}/reminders/{reminderId}": {
      "schema": {
        "$ref": "#/entities/Reminder"
      },
      "description": "A subcollection for a user's reminders. For now, we only use a single document with the ID 'main'."
    },
    "/users/{userId}/events/{eventId}": {
      "schema": {
        "$ref": "#/entities/UserEvent"
      },
      "description": "A subcollection for a user's custom calendar events."
    },
    "/posts/{postId}": {
      "schema": {
        "$ref": "#/entities/Post"
      },
      "description": "A collection of all posts shared by the community. The document ID is auto-generated."
    },
    "/posts/{postId}/comments/{commentId}": {
      "schema": {
        "$ref": "#/entities/Comment"
      },
      "description": "A subcollection for comments on a specific post."
    },
    "/snaps/{snapId}": {
      "schema": {
        "$ref": "#/entities/Snap"
      },
      "description": "A collection of all snaps shared by the community. The document ID is auto-generated."
    }
  }
}
